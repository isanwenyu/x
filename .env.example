# DingTalk机器人配置
# 获取方式：钉钉群 -> 群设置 -> 智能群助手 -> 添加机器人 -> 自定义机器人
DINGTALK_ACCESS_TOKEN=your-dingtalk-token-here

# Twitter/X监控配置
# 需要监控的Twitter/X用户名（不带@符号）
X_USERNAME=elonmusk

# 轮询间隔（秒）
X_POLL_SECONDS=60

# Nitter镜像站点列表（逗号分隔）
# 如果某个站点不可用，会自动切换到下一个
NITTER_BASE_URLS=https://nitter.poast.org,https://nitter.privacydev.net,https://nitter.cz,https://nitter.net,https://nitter.it

# XTracker API配置
# 用于获取Twitter/X账户的统计数据
XTRACKER_URL=https://www.xtracker.io/api/users?stats=true&platform=X

# 时区设置
TIMEZONE=Asia/Shanghai

# 数据存储路径
STATE_DIR=./data/state
LOG_DIR=./data/logs

# 首次运行时是否推送历史推文
# true: 推送所有历史推文（可能很多）
# false: 只推送新推文
ON_FIRST_RUN_PUSH_ALL=false

# 日志级别
# DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# 请求超时时间（秒）
REQUEST_TIMEOUT=30

# 重试次数
MAX_RETRIES=3

# 重试间隔（秒）
RETRY_DELAY=5

# 是否启用调试模式
DEBUG=false